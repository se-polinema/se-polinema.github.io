---
import VSCodeLayout from '../../layouts/VSCodeLayout.astro'
import { withBase } from '../../lib/paths'
---

<VSCodeLayout title="Alumni">
  <meta slot="head" http-equiv="refresh" content={`0; url=${withBase('/members?filter=alumni')}`} />
  <link slot="head" rel="canonical" href={withBase('/members?filter=alumni')} />
  <div class="px-8 py-5">
    <p class="text-neutral-500 dark:text-gray-400 text-sm">
      Redirecting to <a class="text-accent" href={withBase('/members?filter=alumni')}>Members</a>...
    </p>
  </div>
</VSCodeLayout>
