export interface Achievement {
  title: string
  titleId?: string
  date: string
  type: string
  description?: string
  descriptionId?: string
  url?: string
  issuer?: string
  members?: string[]
}
